# ====================
# 环境变量配置示例文件
# ====================
# 复制此文件为 .env.local 并填入您的实际值

# ===== 基础配置 =====
# Node环境 (development | production | test)
NODE_ENV=development

# ===== 前端API配置 =====
# 前端API地址 (使用代理模式，通常保持默认值)
NEXT_PUBLIC_API_URL=/api/proxy

# ===== 后端服务配置 =====
# 知识库服务完整地址 (请修改为您的实际后端地址)
# 开发环境示例: http://localhost:8000
# 生产环境示例: https://your-api-domain.com
# Docker环境示例: http://backend-container:8000
# K8s环境示例: http://backend-service:8000
API_BASE_URL=http://localhost:8000

# 训练服务完整地址 (专用于模型训练和GPU状态查询)
# 用于处理 /api/v1/unified_training/ 相关请求
TRAINING_API_BASE_URL=http://localhost:8001

# ===== 认证配置 =====
# 默认认证Token (可选，如果后端需要认证)
# 如果不需要认证，可以留空或删除此行
DEFAULT_TOKEN=

# ===== 网络配置 (可选) =====
# 代理配置 - 如果需要通过代理访问后端服务
# HTTP_PROXY=http://your-proxy:port
# HTTPS_PROXY=https://your-proxy:port
# NO_PROXY=localhost,127.0.0.1

# ===== 开发配置 =====
# API请求超时时间 (毫秒)
API_TIMEOUT=30000

# 是否启用API日志 (开发环境建议启用)
ENABLE_API_LOGS=true